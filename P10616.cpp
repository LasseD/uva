int main() {
  int N, Q, DP[20][200][10], a[200], D, M;
  for(int cas = 1; true; ++cas) {
    cin >> N >> Q;
    if(N == 0 && Q == 0)
      return 0;
    cout << "SET " << cas << ":" << endl;
    FORI(N)
      cin >> a[i];
    FORI(Q) {
      cin >> D >> M;
      
      // TODO: Compute ret!
    }
  }
}
